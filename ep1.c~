#include <pthread.h>
#include <stdio.h>







int main (int argc, char **argv) {
    int escalonador = atoi (arg[1]);
    char *arquivo   = argv[2];
    char *saida     = argv[3];

    FILE *file;
    file = fopen (arquivo, "r");

    
}
